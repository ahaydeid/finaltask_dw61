import multer from "multer";
// import fs from "fs";
// const uploadDir = "uploads";

export function manageStack(req, res) {
  res.render("manageStack");
}

export function stackHandler(req, res) {
  const sql = ``;
}
